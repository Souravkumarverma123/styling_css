{
  "name": "styling_css",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:lemon-css && vite build",
    "build:lemon-css": "cd lemon-css && npm install && npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.1",
    "rollup": "^4.20.0"
  }
}
